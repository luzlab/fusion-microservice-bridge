This is a Python shim for the Javascript plugin. The onOpenFromURL event is not available in javascript.

This will pop up an alert box displaying the privateInfo parameter whenever an open URL is called.